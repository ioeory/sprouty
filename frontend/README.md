# Sprouty 前端

本目录为 **Sprouty（萌记）** Web 客户端：React 19、Vite、TypeScript、Tailwind CSS v4。

- **完整说明**（部署、环境变量、功能列表）：请阅读仓库根目录 [README.md](../README.md)。
- **本地开发**：`npm install` → `npm run dev`（默认 `:5173`，`/api` 代理到后端 `:8080`）。
- **生产构建**：`npm run build`，产物由根目录 Docker / Nginx 镜像提供。
